# 🏗️ Architettura del Sistema

Questo documento descrive in dettaglio l'architettura tecnica del sistema di fine-tuning LLM.

## 📋 Indice

- [Overview](#overview)
- [Layer 1: Data Ingestion](#layer-1-data-ingestion)
- [Layer 2: Model Loading](#layer-2-model-loading)
- [Layer 3: Training Pipeline](#layer-3-training-pipeline)
- [Layer 4: Memory System](#layer-4-memory-system)
- [Flow di Esecuzione](#flow-di-esecuzione)

---

## Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           SYSTEM OVERVIEW                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌───────────┐ │
│  │   CONFIG    │────▶│   LOADER    │────▶│   TRAINER   │────▶│  OUTPUT   │ │
│  │   (YAML)    │     │  (QLoRA)    │     │ (Lightning) │     │  (LoRA)   │ │
│  └─────────────┘     └─────────────┘     └─────────────┘     └───────────┘ │
│         │                   │                   │                          │
│         │                   │                   │                          │
│         ▼                   ▼                   ▼                          │
│  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐                   │
│  │  DATASETS   │     │    PEFT     │     │   REWARD    │                   │
│  │ Multi-Source│     │    LoRA     │     │  FUNCTIONS  │                   │
│  └─────────────┘     └─────────────┘     └─────────────┘                   │
│                                                                             │
│                         ┌─────────────────────────┐                        │
│                         │     MEMORY SYSTEM       │                        │
│                         │  ┌─────────┬─────────┐  │                        │
│                         │  │   RAG   │   SOP   │  │                        │
│                         │  └─────────┴─────────┘  │                        │
│                         └─────────────────────────┘                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Layer 1: Data Ingestion

### Multi-Source DataModule

Il sistema supporta il caricamento di dataset multipli con pesi diversi per evitare il catastrophic forgetting.

```
                    MULTI-SOURCE DATA PIPELINE
                    ──────────────────────────

    ┌──────────────────────────────────────────────────────────┐
    │                    HuggingFace Hub                       │
    └───────────────────────────┬──────────────────────────────┘
                                │
        ┌───────────────────────┼───────────────────────┐
        │                       │                       │
        ▼                       ▼                       ▼
┌───────────────┐     ┌───────────────┐     ┌───────────────┐
│  Function     │     │    Coding     │     │     Chat      │
│  Calling      │     │   Datasets    │     │   Datasets    │
│   (30%)       │     │    (30%)      │     │    (30%)      │
└───────┬───────┘     └───────┬───────┘     └───────┬───────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │   Formatter     │
                    │    (ChatML)     │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  WeightedSampler│
                    │                 │
                    │ P(dataset_i) =  │
                    │   weight_i /    │
                    │   Σ weights     │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   DataLoader    │
                    └─────────────────┘
```

### Formattazione ChatML

```python
# Input grezzo da dataset
{
    "instruction": "Scrivi una funzione per calcolare il fattoriale",
    "response": "def factorial(n): ..."
}

# Output formattato
<|im_start|>system
You are a helpful AI assistant specialized in coding.<|im_end|>
<|im_start|>user
Scrivi una funzione per calcolare il fattoriale<|im_end|>
<|im_start|>assistant
def factorial(n):
    """Calcola il fattoriale di n."""
    if n <= 1:
        return 1
    return n * factorial(n - 1)<|im_end|>
```

---

## Layer 2: Model Loading

### QLoRA Pipeline

```
                        QLORA LOADING PIPELINE
                        ──────────────────────

    ┌─────────────────────────────────────────────────────────┐
    │                    HuggingFace Hub                      │
    │              (mistralai/Mistral-7B-v0.3)               │
    └───────────────────────────┬─────────────────────────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │   BitsAndBytesConfig  │
                    │   ─────────────────   │
                    │   load_in_4bit=True   │
                    │   quant_type="nf4"    │
                    │   double_quant=True   │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  AutoModelForCausalLM │
                    │       .from_pretrained│
                    │                       │
                    │  device_map="auto"    │
                    │  torch_dtype=float16  │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │ prepare_model_for_    │
                    │    kbit_training()    │
                    │                       │
                    │  • Congela base model │
                    │  • Prepara gradient   │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │      LoraConfig       │
                    │   ─────────────────   │
                    │   r=16                │
                    │   alpha=32            │
                    │   target_modules=[    │
                    │     q_proj, k_proj,   │
                    │     v_proj, o_proj,   │
                    │     gate_proj, ...    │
                    │   ]                   │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │    get_peft_model()   │
                    │                       │
                    │  Trainable: 0.18%     │
                    │  (~13M params)        │
                    └───────────────────────┘
```

### Mapping Target Modules per Architettura

| Architettura | Attenzione | MLP |
|-------------|------------|-----|
| Mistral/Llama | q_proj, k_proj, v_proj, o_proj | gate_proj, up_proj, down_proj |
| GPT-NeoX/Pythia | query_key_value, dense | dense_h_to_4h, dense_4h_to_h |
| GPT-2/GPT-J | c_attn, c_proj | c_fc |

---

## Layer 3: Training Pipeline

### PyTorch Lightning Module

```
                    LIGHTNINGMODULE LIFECYCLE
                    ─────────────────────────

    ┌─────────────────────────────────────────────────────────┐
    │                    training_step()                      │
    │  ─────────────────────────────────────────────────────  │
    │                                                         │
    │   batch = {                                             │
    │       "input_ids": [B, seq_len],                        │
    │       "attention_mask": [B, seq_len],                   │
    │       "labels": [B, seq_len]  # -100 per token ignorati │
    │   }                                                     │
    │                                                         │
    │   outputs = model(**batch)                              │
    │   loss = outputs.loss  # CrossEntropyLoss automatico    │
    │                                                         │
    │   self.log("train/loss", loss)                          │
    │   self.log("train/perplexity", exp(loss))               │
    │                                                         │
    │   return loss                                           │
    │                                                         │
    └─────────────────────────────────────────────────────────┘
                                │
                                ▼
    ┌─────────────────────────────────────────────────────────┐
    │                 configure_optimizers()                   │
    │  ─────────────────────────────────────────────────────  │
    │                                                         │
    │   optimizer = AdamW(                                    │
    │       filter(requires_grad),                            │
    │       lr=2e-4,                                          │
    │       weight_decay=0.01                                 │
    │   )                                                     │
    │                                                         │
    │   scheduler = SequentialLR([                            │
    │       LinearLR(warmup_steps),      # Warmup             │
    │       CosineAnnealingLR(max_steps) # Decay              │
    │   ])                                                    │
    │                                                         │
    └─────────────────────────────────────────────────────────┘
```

### Agent Lightning GRPO

```
                         GRPO ALGORITHM
                         ─────────────
                         
    ┌─────────────────────────────────────────────────────────┐
    │                    Per ogni prompt:                      │
    └───────────────────────────┬─────────────────────────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │   Generate K samples  │
                    │   (temperature > 0)   │
                    │                       │
                    │   y₁, y₂, ..., yₖ     │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  Compute rewards      │
                    │                       │
                    │  r(x, y₁), ..., r(x,yₖ)│
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  Normalize rewards    │
                    │                       │
                    │  Â(yᵢ) = r(yᵢ) - μ    │
                    │         ─────────     │
                    │            σ          │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  Policy gradient      │
                    │                       │
                    │  L = -Σ Â(yᵢ) ×       │
                    │      log π(yᵢ|x)      │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  Add KL penalty       │
                    │                       │
                    │  L_total = L +        │
                    │    β × KL(π‖π_ref)    │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  Update policy        │
                    │  (gradient descent)   │
                    └───────────────────────┘
```

---

## Layer 4: Memory System

### RAG Pipeline con Reranking

```
                    RAG WITH RERANKING PIPELINE
                    ───────────────────────────

    Query: "Come implemento il pattern Observer?"
                                │
                                ▼
                    ┌───────────────────────┐
                    │   Embedding Model     │
                    │ (all-MiniLM-L6-v2)   │
                    │                       │
                    │   q → [384 dims]      │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │      ChromaDB         │
                    │   Vector Search       │
                    │   (cosine similarity) │
                    │                       │
                    │   Top-K = 20          │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │   Cross-Encoder       │
                    │     Reranking         │
                    │                       │
                    │   Per ogni (q, doc):  │
                    │   score = CE(q, doc)  │
                    │                       │
                    │   Top-N = 3           │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │   Format Context      │
                    │                       │
                    │   [1] (0.89) doc1     │
                    │   [2] (0.76) doc2     │
                    │   [3] (0.71) doc3     │
                    └───────────────────────┘
```

### Smart Chunking con tree-sitter

```
                    SMART CHUNKING PIPELINE
                    ───────────────────────

    Source Code (file.py)
                                │
                                ▼
                    ┌───────────────────────┐
                    │   tree-sitter Parse   │
                    │                       │
                    │   code → AST          │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │   Node Extraction     │
                    │                       │
                    │   for node in AST:    │
                    │     if function_def:  │
                    │       extract_chunk() │
                    │     if class_def:     │
                    │       extract_class() │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │   Size Management     │
                    │                       │
                    │   if chunk > max:     │
                    │     split_on_blocks() │
                    │   if chunk < min:     │
                    │     aggregate()       │
                    └───────────┬───────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │   Embedding Text      │
                    │                       │
                    │   # Function: name    │
                    │   # Description: ...  │
                    │   def name(...):      │
                    │       ...             │
                    └───────────────────────┘
```

---

## Flow di Esecuzione

### Training Completo

```
    main_agent_lightning.py
            │
            ▼
    ┌───────────────────┐
    │   load_config()   │
    │   config.yaml     │
    └─────────┬─────────┘
              │
              ▼
    ┌───────────────────┐
    │   ModelLoader()   │
    │   ───────────     │
    │   • Quantize      │
    │   • LoRA          │
    └─────────┬─────────┘
              │
              ▼
    ┌───────────────────┐
    │ create_data_      │
    │    module()       │
    │   ───────────     │
    │   • Load datasets │
    │   • Format        │
    │   • Tokenize      │
    └─────────┬─────────┘
              │
              ▼
    ┌───────────────────┐
    │ AgentLightning    │
    │    Trainer        │
    │   ───────────     │
    │   • GRPO/APO/SFT  │
    │   • Reward fn     │
    │   • Tracing       │
    └─────────┬─────────┘
              │
              ▼
    ┌───────────────────┐
    │   trainer.train() │
    │   ───────────     │
    │   for epoch:      │
    │     for batch:    │
    │       forward()   │
    │       reward()    │
    │       backward()  │
    │       update()    │
    └─────────┬─────────┘
              │
              ▼
    ┌───────────────────┐
    │   save_model()    │
    │   ───────────     │
    │   • LoRA adapter  │
    │   • Config        │
    │   • Tokenizer     │
    └───────────────────┘
```

---

## 🔧 Estensibilità

### Aggiungere un Nuovo Algoritmo

```python
# src/agent/agent_lightning_trainer.py

class CustomAlgorithm:
    """Implementa il tuo algoritmo di training."""
    
    def __init__(self, model, tokenizer, **config):
        self.model = model
        self.tokenizer = tokenizer
        self.config = config
    
    def train_step(self, batch):
        # 1. Forward pass
        outputs = self.model(**batch)
        
        # 2. Calcola loss personalizzata
        loss = self.custom_loss(outputs)
        
        # 3. Backward
        loss.backward()
        
        return {"loss": loss.item()}
```

### Aggiungere una Nuova Reward Function

```python
# src/agent/agent_lightning_trainer.py

@staticmethod
def my_custom_reward(prompt: str, generation: str) -> float:
    """
    Reward function personalizzata.
    
    Returns:
        float tra -1.0 e 1.0
    """
    reward = 0.0
    
    # Implementa la tua logica
    if "keyword" in generation:
        reward += 0.3
    
    # Penalizza comportamenti indesiderati
    if len(generation) < 10:
        reward -= 0.5
    
    return max(-1.0, min(1.0, reward))
```

---

*Documento generato per il portfolio tecnico. Per contribuire, vedi CONTRIBUTING.md*

